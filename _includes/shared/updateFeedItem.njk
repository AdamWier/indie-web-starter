{% macro updateFeedItem(date, content, url, includeAvatar, photo)%}
    <li class="list-group-item h-entry">
        <div hidden>{% include 'hcard.njk' %}</div>
        <div class="row">
            {% if includeAvatar %}
                <div class="col">
                    <img 
                        class="img-fluid avatar m-1 card-img-top u-photo" 
                        alt="A picture of me in a hat, undershirt, black pants, leaning over the camera by the window" 
                        loading="lazy" 
                        decoding="async" 
                        src="/img/me.jpg"
                    >
                </div>
            {% endif %}
            <div class="col grow-col e-content">
                {% if photo.content %}
                <div class="row">
                    <div class="col">
                        <img src="data:{{photo.mimetype}};base64,{{photo.content | base64}}" class="img-fluid">
                    </div>
                </div>
                {% endif %}
                <div class="row">
                    <div class="col">
                        {{content | safe}}
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <span hidden class="dt-published">{{ date | htmlDateString}}</span>
                <span class="text-info fw-bold">
                    {{date | toLocaleTimeString }}
                    {{date.toLocaleDateString()}}
                </span>
            </div>
            <div class="col text-end">
                <a class="u-url" href="{{url}}">Permalink</a>
            </div>
        </div>
    </li>
{% endmacro %}